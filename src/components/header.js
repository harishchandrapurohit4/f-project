import logo from '../assets/logo.png';

function Header() {
  return (
    <header>
      <img width={200} height={200} src={logo } alt="Little Lemon Logo" />
    </header>
  );
}

export default Header;
